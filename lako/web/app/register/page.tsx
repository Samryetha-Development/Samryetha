"use client";
import {FormEvent,useState} from "react";
export default function Register(){const[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget);const body={username:d.get("username"),email:d.get("email"),display_name:d.get("display_name"),password:d.get("password")};const r=await fetch("/api/auth/register",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});if(r.ok){location.href="/login"}else{const x=await r.json();setError(x.error?.message??"Registration failed")}}return <main><section className="auth-wrap"><div className="panel"><div className="eyebrow">Lako</div><h1>Create account</h1><form onSubmit={submit}><label>Display name<input name="display_name" required /></label><label>Username<input name="username" autoComplete="username" required /></label><label>Email<input name="email" type="email" autoComplete="email" required /></label><label>Password<input name="password" type="password" minLength={12} autoComplete="new-password" required /></label>{error&&<div className="form-error">{error}</div>}<button>Create account</button></form><a className="auth-link" href="/login">Already have an account</a></div></section></main>}
